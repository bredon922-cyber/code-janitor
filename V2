<style>
.page-doll-area {
  position: fixed;
  bottom: 60px;
  left: 10px;
  z-index: 1000;
  display: flex;
  flex-direction: column;
  gap: 12px;
  align-items: flex-start;
}

.page-doll {
  display: inline-block;
}

.page-doll img {
  width: 70px;
  height: auto;
  cursor: pointer;
}

.page-doll.mobile-only {
  display: none;
}

@media (max-width: 768px) {
  .page-doll.mobile-only {
    display: inline-block;
  }

  .page-doll.desktop-only {
    display: none;
  }
}
</style>

<div class="page-doll-area">
  <div class="page-doll">
    <img src="https://cdn.discordapp.com/attachments/1099030085882880111/1434701738429911160/25_09_08__BUBBLE__NAOMY__NOFOND.png?ex=6909f25d&amp;is=6908a0dd&amp;hm=72a2835b2f737911ac72af8b0f5da230b5090208464a52be0f707766eec1703c&amp;" alt="Page Doll Loulou">
  </div>
</div>


<style>
/* page elements */
:root {
    --font-display: "Grand Caslon", "Times New Roman", serif;
    --font-body: "Candara", "Segoe UI", sans-serif;
    --ink: #F2F1DF;
    --ink-dark: #484742;
    --bg-mid: #990a16;
    --acc-1: #c8bcbf;
    --acc-2: #397330;
    --acc-3: #89A65D;
    --acc-4: #40000a;
    --bg-deep: #342323;
}
html, body {
    background-color: #331419;
}
* {
    color: var(--ink);
    font-family: var(--font-body);
}
body, button, input, select, textarea {
    font-family: var(--font-body);
}
h1, h2, h3, h4, h5, h6,
.css-9lg76n,
.css-character-name,
.css-username,
.css-USERNAME-ID,
.css-CHARACTER-NAME-ID,
.display-font {
    font-family: var(--font-display);
}
a:link, a:visited {
    color: var(--acc-3);
}
a:hover, a:active {
    color: var(--acc-2);
}
.css-0 {
    background: none !important; 
}
.Btn2-purple:before {
    transition: none;
    background: var(--bg-mid);
}
.Btn2-purple {
    background: var(--acc-4);
}
.css-9lg76n {
    color: var(--ink);
}
</style>

<style>
/* profile card */
.css-1uodvt1 {
    padding-top: 0px;
    flex-direction: column;
    justify-items: center;
    text-align: center;
}

.css-hsi2ui { /* profile photo */
    height: 250px;
    width: 250px;
    border-radius: 10pc;
    border: VALUEpx solid var(--bg-mid);
    box-shadow: none !important;
}

.css-1w58nos {
    box-shadow: none !important;
    border: VALUEpx STYLE var(--bg-mid) !important;
    border-radius: VALUEpx;
    background-color: var(--acc-1);
    font-family: var(--font-body);
    font-size: VALUEpx;
    color: var(--bg-deep);
}
.css-oqdsp6 {
    box-shadow: none !important;
    border: VALUEpx STYLE var(--acc-4) !important;
    border-radius: VALUEpx;
    background-color: var(--acc-4);
    font-family: var(--font-body);
    font-size: VALUEpx;
    color: var(--ink);
}
.css-1n2in4t, .css-8l42c1 {
    background: var(--bg-mid);
    border: VALUEpx STYLE var(--acc-4);
    border-radius: VALUEpx;
}
.css-1n2in4t svg, .css-8l42c1 svg {
    fill: var(--ink);
}

/* username (replace .css-USERNAME-ID with the inspected class) */
.css-USERNAME-ID {
    font-family: var(--font-display);
    font-size: VALUEpx;
    color: var(--ink);
    text-shadow: VALUEpx VALUEpx var(--acc-4);
    -webkit-text-stroke: VALUEpx var(--ink-dark);
}

/* follower count tag */
.css-FOLLOWER-TAG-ID {
    font-family: var(--font-body);
    font-size: VALUEpx;
    color: var(--bg-deep);
    background: var(--acc-1);
    border: VALUEpx solid var(--bg-mid);
}

/* event badges */
.css-hjkukh {
    position: relative;
    justify-content: center;
    filter: saturate(0.9) brightness(1.05);
}

/* follow button */
.Btn:before {
    color: var(--ink);
    background-color: var(--bg-mid);
    font-family: var(--font-body);
}
.Btn {
    background: var(--acc-4);
}

/* center follow button */
.css-y9k5u7 {
    justify-content: center;
}

/* card border & public characters */
.css-PUBLIC-CHAR-ID {
    border: VALUEpx solid var(--bg-mid);
    border-radius: VALUEpx;
}

/* remove default gradient */
.css-REMOVE-GRADIENT-ID {
  background: none;
}
</style>

<style>
/* ===== Barre d’en-tête ===== */
.pp-top-bar,
.profile-top-bar {
  background: var(--acc-4) !important;
  color: var(--ink) !important;
  border-bottom: 1px solid rgba(255, 255, 255, .12) !important;
  backdrop-filter: blur(12px) !important;
}

/* ===== Recherche ===== */
.profile-top-bar-search-box {
  max-width: 640px !important;
}
.profile-top-bar-search-box input::placeholder {
  color: var(--ink) !important;
  opacity: .85 !important;
}

/* ===== Boutons Auth ===== */
.pp-top-bar-auth-buttons,
.profile-top-bar-auth-buttons {
  display: flex;
  gap: .5rem;
}

.pp-top-bar-auth-buttons-login,
.profile-top-bar-auth-buttons-login {
  background: var(--acc-2) !important;
  color: var(--bg-deep) !important;
  border: 1px solid color-mix(in oklab, var(--acc-2) 80%, black) !important;
  border-radius: 10px !important;
  font-weight: 700 !important;
}
.pp-top-bar-auth-buttons-login:hover,
.profile-top-bar-auth-buttons-login:hover {
  filter: saturate(1.1) brightness(1.05);
}

.pp-top-bar-auth-buttons-register,
.profile-top-bar-auth-buttons-register {
  background: color-mix(in oklab, var(--bg-mid) 85%, var(--acc-4)) !important;
  color: var(--ink) !important;
  border: 1px solid color-mix(in oklab, var(--bg-mid) 65%, black) !important;
  border-radius: 10px !important;
  font-weight: 700 !important;
}
.pp-top-bar-auth-buttons-register:hover,
.profile-top-bar-auth-buttons-register:hover {
  filter: saturate(1.08) brightness(1.04);
}

/* ===== Mobile ===== */
.pp-top-bar-title,
.profile-top-bar-title {
  color: var(--ink) !important;
  font-weight: 800 !important;
  text-shadow: 0 0 10px color-mix(in oklab, var(--ink) 16%, transparent);
}

.pp-top-bar-back-button,
.profile-top-bar-back-button {
  background: var(--ink) !important;
  color: var(--acc-4) !important;
  border: 1px solid rgba(255, 255, 255, .14) !important;
  border-radius: 10px !important;
}

/* ===== Créer un personnage ===== */
.pp-top-bar-right a[href*="create"],
.profile-top-bar-right a[href*="create"] {
  background: var(--ink) !important;
  color: var(--acc-4) !important;
  opacity: .92 !important;
  padding: .45rem .95rem !important;
  border-radius: 10px !important;
  font-weight: 1000 !important;
  text-decoration: none !important;
}
</style>
